/***************************************** User Interface Handling *****************************************/

#pragma once

#include "buffer.hpp"

// Performs user selected operations, returns false if user decides to quit
bool process_user_commands(Buffer &buffer);